# ox-migration-tools
Backup data (calendar, tasks, contacts) from OX apposite via HTTP api

ox_http_api_dataextract.sh - this scipt download your Backup data (calendar, tasks, contacts) from OX appsuite endpoint using your login/password (it is also works with apppasswords if you have export permissions)
